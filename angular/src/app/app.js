angular.module( 'moped', [
  'templates-app',
  'templates-common',
  'moped.home'
])

.config( function myAppConfig ( ) {
})

.run( function run () {
})

.controller( 'AppCtrl', function AppCtrl ( $scope, $location ) {
  
});